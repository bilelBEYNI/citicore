{% extends '/front/index.html.twig' %}

{% block title %}Edit Association{% endblock %}

{% block body %}
    {% include 'front/navbar.html.twig' %}

    <div class="container my-5">
        <h1 class="text-center mb-4">Modifier l'Association</h1>

        <div class="mx-auto" style="max-width: 700px;">
            <div class="card shadow p-4">
                {{ include('association/_form.html.twig', {'button_label': 'Update'}) }}

                <div class="d-flex justify-content-between mt-4">
                    <a href="{{ path('app_association_index') }}" class="btn btn-secondary">Back to list⬅️</a>
                    {{ include('association/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
