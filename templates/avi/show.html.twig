{% extends 'base.html.twig' %}

{% block title %}Avi{% endblock %}

{% block body %}
    <h1>Avi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Avis_id</th>
                <td>{{ avi.avisId }}</td>
            </tr>
            <tr>
                <th>Utilisateur_id</th>
                <td>{{ avi.UtilisateurId }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ avi.commentaire }}</td>
            </tr>
            <tr>
                <th>Date_avis</th>
                <td>{{ avi.dateAvis ? avi.dateAvis|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Demande_id</th>
                <td>{{ avi.DemandeId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_avi_index') }}">back to list</a>

    <a href="{{ path('app_avi_edit', {'avis_id': avi.avis_id}) }}">edit</a>

    {{ include('avi/_delete_form.html.twig') }}
{% endblock %}
