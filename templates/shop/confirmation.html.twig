{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link rel="stylesheet" href="{{ asset('front/css/produit/cart.css') }}?v=1.0">
{% endblock %}

{% block body %}
<div class="container">
    <h2>📜 Confirmation de Commande</h2>
    <div class="row">
        <div class="col-md-8">
            <h4>Résumé de la commande</h4>
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>Produit</th>
                        <th>Quantité</th>
                        <th>Prix unitaire</th>
                        <th>Sous-total</th>
                    </tr>
                </thead>
                <tbody>
                    {% for item in items %}
                        <tr>
                            <td>{{ item.produit.nom }}</td>
                            <td>{{ item.quantity }}</td>
                            <td>{{ item.produit.prix|number_format(2, '.', ',') }} €</td>
                            <td>{{ item.sousTotal|number_format(2, '.', ',') }} €</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>

            <div class="summary">
                <div class="summary-line">
                    <span>Total HT</span>
                    <span>{{ totalHT|number_format(2, '.', ',') }} €</span>
                </div>
                <div class="summary-line">
                    <span>TVA (20%)</span>
                    <span>{{ vatAmount|number_format(2, '.', ',') }} €</span>
                </div>
                <hr>
                <div class="summary-line fw-bold">
                    <span>Total TTC</span>
                    <span>{{ totalTTC|number_format(2, '.', ',') }} €</span>
                </div>
            </div>

            <div class="d-flex gap-2 mt-3">
                <form action="{{ path('app_create_checkout_session') }}" method="POST">
                    <button type="submit" class="btn btn-success">Procéder au paiement</button>
                </form>

                <form action="{{ path('annuler_commande') }}" method="POST">
                    <button type="submit" class="btn btn-danger">Annuler la commande</button>
                </form>
            </div>
        </div>
        <div class="col-md-4">
            <div class="alert alert-info">
                <p><strong>Livraison :</strong> 5.00 €</p>
                <p>Total à payer : <strong>{{ (totalTTC + 5)|number_format(2, '.', ',') }} €</strong></p>
                <a href="{{ path('generate_pdf') }}" target="_blank" class="btn btn-outline-primary">
                    Télécharger le reçu PDF
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
