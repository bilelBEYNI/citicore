<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Confirmation de Commande | Ma Boutique</title>
    <link rel="stylesheet" href="{{ asset('styles/css/bootstrap.min.css') }}">
    <style>
        body {
            background: #f0f2f5;
            font-family: "Segoe UI", Roboto, sans-serif;
            color: #333;
        }
       .d-flex.mt-4 > form + form {
        margin-left: 1rem;
    }
        h2 {
            color: #6f42c1;
            font-weight: 700;
        }

        .card-modern {
            background: #fff;
            border-radius: 16px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.08);
            padding: 30px;
            margin-top: 20px;
        }

        table th {
            background: #f4ecfb;
            color: #6f42c1;
        }

        .summary-line {
            font-size: 1rem;
            margin: 10px 0;
        }

        .summary-line strong {
            font-weight: 600;
        }

        .btn-modern {
            border-radius: 12px;
            font-weight: 600;
            padding: 10px 20px;
            border: none;
            transition: all 0.3s ease-in-out;
        }

        .btn-purple {
            background-color: #9B59B6;
            color: white;
        }

        .btn-purple:hover {
            background-color: #8e44ad;
        }

        .btn-lightblue {
            background-color: #6CB2EB;
            color: white;
        }

        .btn-lightblue:hover {
            background-color: #0056B3;
        }

        .btn-outline {
            background-color: transparent;
            border: 2px solid #ccc;
            color: #333;
        }

        .btn-outline:hover {
            background-color: #f8f9fa;
        }

        .total-box {
            background-color: #f8f9fc;
            border: 1px solid #ddd;
            border-radius: 12px;
            padding: 20px;
        }

        .total-box strong {
            color: #6f42c1;
        }

        .w-100 {
            width: 100%;
        }
    </style>
</head>
<body>
    <div class="container py-5" style="max-width: 1100px;">
        <h2 class="mb-4">📜 Confirmation de Commande</h2>
        <div class="row">
            <div class="col-md-8">
                <div class="card-modern">
                    <h4 class="mb-4">🛍️ Résumé de la commande</h4>

                    <table class="table table-bordered rounded">
                        <thead>
                            <tr>
                                <th>Produit</th>
                                <th>Quantité</th>
                                <th>Prix unitaire</th>
                                <th>Sous-total</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for item in items %}
                                <tr>
                                    <td>{{ item.produit.nom }}</td>
                                    <td>{{ item.quantity }}</td>
                                    <td>{{ item.produit.prix|number_format(2, '.', ',') }} €</td>
                                    <td>{{ item.sousTotal|number_format(2, '.', ',') }} €</td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>

                    <div class="mt-4">
                        <div class="summary-line d-flex justify-content-between">
                            <span>Total HT</span>
                            <span><strong>{{ totalHT|number_format(2, '.', ',') }} €</strong></span>
                        </div>
                        <div class="summary-line d-flex justify-content-between">
                            <span>TVA (20%)</span>
                            <span><strong>{{ vatAmount|number_format(2, '.', ',') }} €</strong></span>
                        </div>
                        <hr>
                        <div class="summary-line d-flex justify-content-between">
                            <span>Total TTC</span>
                            <span><strong>{{ totalTTC|number_format(2, '.', ',') }} €</strong></span>
                        </div>
                    </div>

                                <div class="d-flex gap-3 mt-4">
                                 <form action="{{ path('app_create_checkout_session') }}" method="POST">
                                 <button type="submit" class="btn btn-modern btn-purple">💳 Procéder au paiement</button>
                                </form>
                                <form action="{{ path('annuler_commande') }}" method="POST">
                               <button type="submit" class="btn btn-modern btn-lightblue">❌ Annuler la commande</button>
                                  </form>
</div>


                </div>
            </div>

            <div class="col-md-4">
                <div class="total-box shadow-sm mt-4 mt-md-0">
                    <p><strong>Livraison :</strong> 5.00 €</p>
                    <p>Total à payer : <strong>{{ (totalTTC + 5)|number_format(2, '.', ',') }} €</strong></p>
                    <a href="{{ path('app_cart_pdf') }}" class="btn btn-modern btn-outline w-100 mt-3">📄 Télécharger PDF</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
