{% extends 'back/Dashboard.html.twig' %}

{% block title %}Ajouter une Réponse{% endblock %}

{% block body %}
<div class="container my-5" style="margin-left: 300px; padding-bottom: 50px;">
    <div class="card shadow-lg border-0 rounded-4">

        <div class="card-header bg-info text-white rounded-top-4">
            <h4 class="mb-0">
                💬 Ajouter une réponse à la réclamation 
                <span class="badge bg-light text-dark">{{ reclamation.ID_Reclamation }}</span>
            </h4>
        </div>

        <<div class="card-body px-4 py-4">
            {{ form_start(form) }}

                {# Exemple de structure de chaque champ dans un bloc individuel #}
                {% for field in form %}
                    <div class="form-group mb-3">
                        {{ form_label(field, null, {'label_attr': {'class': 'form-label fw-semibold'}}) }}
                        {{ form_widget(field, {'attr': {'class': 'form-control'}}) }}
                        {{ form_errors(field) }}
                    </div>
                {% endfor %}

                <div class="text-end mt-4">
                    <button type="submit" class="btn btn-info px-4 py-2 fw-semibold shadow-sm">
                        ➕ Répondre
                    </button>
                </div>

            {{ form_end(form) }}
        </div>
    </div>
</div>

<style>
.btn-info:hover {
    background-color: #17a2b8 !important;
    box-shadow: 0 0 10px rgba(23, 162, 184, 0.5);
    transform: translateY(-2px);
    transition: all 0.3s ease;
}
</style>

{% endblock %}
