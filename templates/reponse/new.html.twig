{% extends '/front/index.html.twig' %}

{% block title %}New Reponse{% endblock %}

{% block body %}
   <div class="container mt-5">
    <div class="card shadow">
        <div class="card-header bg-info text-white">
            <h4 class="mb-0">➕ Ajouter une réponse à : <em>{{ reclamation.sujet }}</em></h4>
        </div>

    {{ include('reponse/_form.html.twig') }}

    <div class="d-flex justify-content-between">
        <button class="btn btn-info">💬 Répondre</button>
        <a href="{{ path('app_reponse_index')}}" class="btn btn-secondary">⬅ Retour</a>
    </div>

{% endblock %}
