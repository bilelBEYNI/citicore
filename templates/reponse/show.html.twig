{% extends '/front/index.html.twig' %}

{% block title %}Détail Reponse{% endblock %}

{% block body %}
{% include 'front/navbar.html.twig' %}
    <h1>Reponse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID_Reponse</th>
                <td>{{ reponse.ID_Reponse }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ reponse.Contenu }}</td>
            </tr>
            <tr>
                <th>DateReponse</th>
                <td>{{ reponse.DateReponse ? reponse.DateReponse|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ reponse.Statut }}</td>
            </tr>
        </tbody>
    </table>

    <div class="mt-4">
        <a href="{{ path('app_reponse_index') }}" class="btn btn-secondary">⬅ Retour à la liste</a>

        
    </div>
    {{ include('reponse/_delete_form.html.twig') }}
{% endblock %}
