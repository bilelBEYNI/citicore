{% extends 'base.html.twig' %}

{% block title %}Demande{% endblock %}

{% block body %}
    <h1>Demande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Demande_id</th>
                <td>{{ demande.DemandeId }}</td>
            </tr>
            <tr>
                <th>Utilisateur_id</th>
                <td>{{ demande.UtilisateurId }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ demande.contenu }}</td>
            </tr>
            <tr>
                <th>Date_demande</th>
                <td>{{ demande.dateDemande ? demande.dateDemande|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ demande.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_demande_index') }}">back to list</a>

    <a href="{{ path('app_demande_edit', {'Demande_id': demande.Demande_id}) }}">edit</a>

    {{ include('demande/_delete_form.html.twig') }}
{% endblock %}
